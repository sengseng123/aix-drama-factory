# MiniMax H3 短剧工厂 V2 实验版

## 环境边界

- 正式版：本机已有的稳定整合包目录
- 开发版：本仓库所在目录
- 开发端口：Web 7862、ComfyUI 8190、Qwen 8085。正式 Web 默认使用 7861，开发版通过独立端口避免误连正式实例。
- 正式版的模型文件仅供开发版读取；项目、资产、输出、配置和日志不共享。

## 启动

双击 `start-dev.bat`。如果正式 ComfyUI 8189 或 Qwen 8084 正在运行，启动器会阻止开发版启动，且不会结束正式进程。

开发版 Qwen 使用 `llm\qwen4b\models` 目录联接，只读共享正式版 `llm\qwen4b\models`。自动启动由 `app.py` 直接调用 `llama-server.exe`，避免 `cmd.exe` 解析中文模型路径失败；手动启动可用根目录 `start_qwen4b.bat`。

首次部署时，将 `config.example.json` 复制为 `config.json`，并将 `extra_model_paths.example.yaml` 复制为 `extra_model_paths.yaml` 后填写本机路径。这两个本机配置文件不会进入 Git。

启用显存互斥时，剧情大纲、剧本和提示词阶段会先确认 8190 队列为空，再切换到 Qwen 8085；资产图或视频阶段会关闭 Qwen 后恢复 ComfyUI。任何正在运行或等待的 ComfyUI 任务都不会被自动中断。

切回正式版前，先双击 `stop-dev.bat`。它只会结束路径属于开发版且监听7862、8190或8085的进程，不会操作正式端口。

`start-dev.bat` 固定使用本机 `127.0.0.1:7862`；如果该端口已被占用，启动器会明确停止并提示处理，不会把未知服务当作当前开发实例。直接运行 `python app.py` 时也应确保所选端口未被其他 AIX 实例占用。

## 迭代规则

视频参考一期的配置与回归入口见 [VIDEO-REFERENCE.md](VIDEO-REFERENCE.md)。它使用独立视觉服务，默认地址仅为示例，不会自动启动模型。

1. 每次只处理一个真实使用问题。
2. 先记录复现步骤、预期结果和实际结果。
3. 修改后执行语法、接口、页面和对应流程回归。
4. 用户实际验收后，才将问题标记为已接受。
5. 普通实验不回移正式版；P0修复须在开发版验证并单独备份后才能回移。

## 模型安全

不要在开发版 ComfyUI Manager 中安装、更新、移动或删除共享模型。新增实验模型应放入开发版自己的 `ComfyUI\ComfyUI\models`。
