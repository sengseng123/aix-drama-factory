# 视频参考创作（一期 / Phase A）

关联 #12、#13。本期把参考视频的镜头结构整理成可编辑计划，再导入现有 H3 制作流程。动作驱动和混合模式属于 #14，不在本期实现。

## 使用流程

1. 首页点击「视频参考创作」。新页面会建立独立参考草稿，后续可从项目列表继续打开。
2. 展开视觉分析设置，配置独立的本地或云端视觉服务。两者都使用支持多图输入的 OpenAI 兼容 `/v1/chat/completions` 协议；地址填写到 `/v1`，模型名填写服务实际提供的名称。本地示例端口 8086 不代表程序会自动启动模型。本地只接受回环地址，云端要求 HTTPS；不自动借用现有文案模型，也未适配 Gemini 原生协议。
3. 配置 FFmpeg，并准备同目录或 PATH 中的 ffprobe，也可单独填写 ffprobe 路径。上传 MP4/MOV，默认限制 300 MB、60 秒，可在设置调整。原文件保留在本机；预览代理最长边 960、24 fps、无音轨，自动处理旋转，时间统一为源视频起点偏移，支持可变帧率输入。
4. 选择起止秒数，选择本地或云端分析。按场景变化检测切镜，每镜头抽取 15%、50%、85% 位置的三帧，最长边 512 像素；模型分析动作描述、镜头设计、色调及待复核事项。最多 60 个参考镜头。云端每次需要确认当前地址、模型及选区，只发送抽帧和时间信息，不发送音频或整段视频。
5. 显式应用候选分析，填写自己的故事、角色外貌、场景及每镜头目标动作。原片人物只使用临时编号，台词不自动复制。可调整顺序、时间、拆分、合并、删除镜头，并选择是否参考镜头、节奏、色调、动作结构。这里的「动作结构」是文字提示，不是姿态或视频驱动。
6. 相邻镜头使用相同生产分组可合成片段。长镜头拆分到不超过 15 秒；H3 最小时长为 3 秒。保存后查看源时间、计划时长、对齐帧数及差异说明；不对源视频暗中变速。
7. 先试做首片，或导入全部片段。导出创建独立制作项目并进入剧本审核，之后继续现有提示词、资产、视频生成流程。自己的角色图在资产审核中上传，源截图不会自动作为人物资产。

首片测试与完整制作是独立项目，测试成片不会自动复用到完整项目。同一已保存方案、同一导出模式重复导入会打开已有制作项目；修改方案后生成新项目，保留旧成片。制作项目的 `reference_snapshot` 是导入时的参考来源快照，后续在制作页改剧本或参数不会回写参考草稿；实际生成参数仍以制作项目为准。

## 保存、停止和重试

- 源视频、代理、抽帧和中间分析保存在 `projects/.references/<草稿ID>/`，草稿编辑状态在项目 JSON；均沿用忽略规则，不提交 Git。
- 已保存编辑有版本冲突检查；浏览器保留未保存草稿，跨浏览器需先保存。重新分析只产生候选版本，不覆盖人工编辑；应用候选版本会替换镜头卡片。
- 导入和分析复用项目任务锁。停止只作用于本次任务和自有媒体子进程，已完成镜头的分析缓存保留；相同参数重试跳过已完成镜头。重启后中断任务标记失败，不自动重新请求模型。
- 停止视觉请求是协作式的：正在阻塞的网络读取须返回或超时后才能结束，界面显示「正在停止」。文件上传阶段尚无页面取消按钮，上传完成后的媒体处理可停止。
- 缓存按源文件指纹、选区、服务地址、模型及分析器版本隔离。同名模型换权重后，修改「分析模型版本标记」以触发新分析。Key 不写入缓存，配置接口不回显 Key。
- 历史分析、源视频和旧输出暂不自动清理。磁盘空间不足时，应先确认哪些项目仍引用这些材料，再自行清理；删除草稿不会自动级联删除所有引用素材。

## 接口与结构

实现分为 `reference_media.py`（媒体处理、计划）、`reference_api.py`（独立 Blueprint、保存及任务）、`static/reference.html` / `reference.js`（编辑界面）。打包时需要包含这些文件。

| 接口 | 用途 |
| --- | --- |
| GET/POST `/api/reference/config` | 查询/更新独立视觉配置 |
| POST `/api/reference/projects` | 创建参考草稿 |
| GET `/api/reference/<pid>` | 状态、候选分析、已保存计划 |
| POST `/api/reference/<pid>/upload`、`retry-import` | 上传、重试媒体处理 |
| POST `/api/reference/<pid>/analyze` | 选区分析，云端附当前传输确认 |
| POST `/api/reference/<pid>/apply-analysis`、`edit`、`export` | 携带 revision 的编辑操作 |
| GET `/api/reference/<pid>/media/<path>` | 受草稿目录约束的预览/抽帧 |
| POST `/api/project/<pid>/stop` | 沿用原有 run_id 停止接口 |

## 验证与当前限制

```bash
python -m unittest discover -s tests -v
node --check static/reference.js
```

真实媒体回归可设置环境变量 `AIX_TEST_FFMPEG`、`AIX_TEST_FFPROBE` 指向本机工具，再运行完整测试或 `python -m unittest discover -s tests -p test_reference_media_integration.py -v`。测试即时生成合成素材，不使用客户视频，不需要模型。旋转素材测试需要支持 `-display_rotation` 的 FFmpeg；本次验证使用 FFmpeg / ffprobe 9.0.1。

模型协议测试使用本地模拟 HTTP 服务，不能替代真实视觉模型的识别效果验证。三张抽帧可能漏掉短促动作；固定切镜阈值可能漏检渐变或产生误切，必须人工检查。尚需真实视觉服务、ComfyUI/H3 出片及浏览器完整交互验收；不将自动化测试通过视为这些验收已完成。
